# 공통 의존성 (학습/추론 양쪽). torch는 환경별 파일 참조:
#   - Elice A100 학습: requirements-train.txt
#   - Jetson 추론:     requirements-jetson.txt
numpy==1.26.4
scipy>=1.10
soundfile>=0.12
PyYAML>=6.0
tqdm>=4.66
matplotlib>=3.7
# 지연·정렬·대역·플랜트지문 같은 "두 곳에서 유도되면 안 되는 값"을 타입으로 강제한다.
# 결함 이력의 78%가 같은 물리량을 여러 곳에서 따로 유도해 어긋난 데서 나왔다.
# 경계(설정·산출물·게이트)에서만 쓰고 오디오 콜백 핫패스에는 넣지 않는다 (검증 1.4us/건 실측).
pydantic>=2.9,<3
